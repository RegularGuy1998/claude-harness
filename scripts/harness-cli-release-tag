# Pinned harness-cli release tag. The CLI is vendored in crates/harness-cli and
# built + published from this repo's own GitHub Releases
# (see .github/workflows/harness-cli-release.yml).
# Bump in lockstep with crates/harness-cli/Cargo.toml. Override at runtime with
# HARNESS_CLI_RELEASE_TAG, HARNESS_CLI_BASE_URL (mirror / offline copy), or
# HARNESS_CLI_RELEASE_REPO (a different host repo).
# The first non-comment token on a line is used.
harness-cli-v0.1.11
